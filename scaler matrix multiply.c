
#include <stdio.h>

int main() {
   int arr[4];
   
   for(int i=0; i<4; i++){
       printf("Enter the value of %d element in the array\n",i);
       scanf("%d",&arr[i]);
   }
   for(int i=0; i<4; i++)
           printf("the value of %d element of the array is %d\n",i,arr[i]);
   }
    
   
    return 0;
}
